{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tramitejubilacion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tramiteJubilacion.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tramitejubilacion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tramitejubilacion_edit', { 'id': tramiteJubilacion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
