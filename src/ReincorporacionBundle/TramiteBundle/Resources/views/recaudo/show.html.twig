{% extends 'base.html.twig' %}

{% block body %}
    <h1>Recaudo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recaudo.id }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ recaudo.path }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ recaudo.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('recaudo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('recaudo_edit', { 'id': recaudo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
