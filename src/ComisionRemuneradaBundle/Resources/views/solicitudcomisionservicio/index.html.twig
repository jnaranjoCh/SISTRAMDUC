{% extends 'base.html.twig' %}

{% block content %}
    <h1>Solicitudcomisionservicios list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for solicitudComisionServicio in solicitudComisionServicios %}
            <tr>
                <td><a href="{{ path('solicitudcomisionservicio_show', { 'id': solicitudComisionServicio.id }) }}">{{ solicitudComisionServicio.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('solicitudcomisionservicio_show', { 'id': solicitudComisionServicio.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('solicitudcomisionservicio_edit', { 'id': solicitudComisionServicio.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('solicitudcomisionservicio_new') }}">Create a new solicitudComisionServicio</a>
        </li>
    </ul>
{% endblock %}
